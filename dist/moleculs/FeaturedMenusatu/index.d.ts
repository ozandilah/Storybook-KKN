import { ReactNode } from "react";
type Props = {
    title: string;
    image: string | ReactNode;
    preimage?: string;
};
declare function FeaturedMenuSatu({ title, image, preimage }: Props): import("react/jsx-runtime").JSX.Element;
export default FeaturedMenuSatu;
