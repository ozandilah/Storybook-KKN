import { Meta, Canvas, Story, Source } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import "assets/css/index.css";
import FeaturedMenuItem from "./example/Sample";

<Meta title="Components/FeaturedMenuItem" />

# FeaturedMenuItem

A most import components that simply let user interact with the app,[[Default Example](/docs/components-FeaturedMenuItem-example--default)] if you want to see in action

export const Template = (args) => <FeaturedMenuItem {...args} />;

<Story name="Introduction" args={{}}>
  {Template.bind({})}
</Story>
