import { Meta, Canvas, Story, Source } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import "assets/css/index.css";
import SectionDua from "./example/Sample";

<Meta title="Components/SectionDua" />

# SectionDua

A most import components that simply let user interact with the app,[[Default Example](/docs/components-SectionDua-example--default)] if you want to see in action

export const Template = (args) => <SectionDua {...args} />;

<Story name="Introduction" args={{}}>
  {Template.bind({})}
</Story>
